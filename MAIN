# Main Application
if __name__ == "__main__":
    root = tk.Tk()
    app = DentalApp(root)
    root.mainloop()

explanation
Finally, the main entry point for the Tkinter app is as follows:

- This is where the Tkinter window starts, and the DentalApp object is instantiated, running the GUI.
